@page
@model MychessProj.Pages.PCity.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.City[0].NameCity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.City) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameCity)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CityCode">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CityCode">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CityCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
